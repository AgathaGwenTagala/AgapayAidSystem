@page
@using MySql.Data.MySqlClient;

@{
	try
	{
		String id = Request.Query["id"];

		string connectionString = "server=localhost;user=root;database=agapayaid;port=3306;password=12345;";
		using (MySqlConnection connection = new MySqlConnection(connectionString))
		{
			connection.Open();
			String sql = "DELETE * FROM evacuation_center WHERE id=@centerID";
			using (MySqlCommand command = new MySqlCommand(sql, connection))
			{
				command.Parameters.AddWithValue("@centerID", id);

				command.ExecuteNonQuery();
			}
		}
	}

	catch (Exception ex)
	{
	}

	Response.Redirect("/EvacuationCenter/Index");
}
